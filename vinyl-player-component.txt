VINYL PLAYER COMPONENT — COMPLETE HANDOFF
==========================================

Source project: Naive Tattoo Dresden
Location on page: Homepage hero (index.html)


1. WHAT THE COMPONENT DOES
--------------------------

The vinyl player is an interactive, layered image composition. The wooden player
is the base image, the record is placed and scaled above it, and the tonearm is a
separate transparent PNG positioned above both. A transparent HTML button sits
over the start/stop control printed in the player artwork.

The visitor can start and stop the music in two ways:

- Drag the tonearm from its parked angle (-38 degrees) toward the record. Releasing
  it at or beyond the -26-degree threshold starts playback and moves it to the
  playing angle (-15 degrees).
- Click the invisible start/stop hit area on the player artwork.

While playing, the vinyl rotates once every 3.8 seconds and the MP3 loops. Playback
state and the current time are stored in sessionStorage, so the player can resume
when the visitor returns to the page during the same browser session. Audio volume
is set to 40%. Browser autoplay restrictions are handled by trying again after the
visitor's next pointer interaction.

The tonearm also supports the Enter and Space keys. ARIA labels and aria-pressed
states expose the controls to assistive technology.


2. ASSETS
---------

All visual source files are in: Assets/Vinyl player/

- player.png — 1536 x 1024 px
  Base image of the complete wooden record-player body. Used on the live page.

- vinyl.png — 1254 x 1254 px
  Record/label layer. Used on the live page and rotated during playback.

- stick.png — 1254 x 1254 px
  Transparent tonearm/needle layer. Used on the live page as the draggable control.

- start stop button.png — 1254 x 1254 px
  Separate start/stop-button artwork/reference. The live implementation currently
  uses a transparent HTML hit area over the button already shown in player.png.

- Tattoo gun.png — 1448 x 1086 px
  Alternate/decorative tattoo-gun artwork supplied with the vinyl-player assets.
  It is not referenced by the current live component.

- Expected when off.png — 1254 x 1254 px
  Visual reference for the assembled player in its stopped/parked state.

- Expected when on.png — 1254 x 1254 px
  Visual reference for the assembled player in its playing state.

- assembly guide.png — 1536 x 1024 px
  Assembly/layout reference image.

- Guide to assemble.png — 1536 x 1024 px
  Additional assembly/layout reference image.

Audio file:

- Assets/Music/Rock Wit U (Yoruba Soul Mix - Dub).mp3
  Looping record audio used by the live component (14,313,149 bytes).


3. HTML (FROM index.html)
-------------------------

<div class="deck-area">
  <div class="deck" id="deck" aria-label="Interactive record player">
    <img class="player" src="Assets/Vinyl player/player.png?v=221256" alt="Wooden record player">
    <div class="vinyl-wrap">
      <img class="vinyl" src="Assets/Vinyl player/vinyl.png" alt="Naive Tattoo vinyl record">
    </div>
    <button class="tonearm" id="tonearm" type="button"
      aria-label="Hold and drag the tonearm onto or off the record"
      aria-pressed="false">
      <img src="Assets/Vinyl player/stick.png" alt="">
    </button>
    <button class="start-stop-hit" id="startStopButton" type="button"
      aria-label="Start or stop the record" aria-pressed="false"></button>
  </div>
  <div class="move-prompt" aria-hidden="true">
    <span><b>Move the needle</b></span>
  </div>
</div>

<span class="scroll-note">Drag the needle to play <span>&nearr;</span></span>

<audio id="recordAudio" preload="metadata" loop
  src="Assets/Music/Rock Wit U (Yoruba Soul Mix - Dub).mp3"></audio>

The page must also load script.js after this markup:

<script src="script.js"></script>


4. CSS — COMPONENT STYLES (FROM styles.css)
------------------------------------------------

.deck-area{position:relative;width:min(64vw,760px);justify-self:end;margin-right:-2vw}
.deck{position:relative;aspect-ratio:1;user-select:none;touch-action:none}
.deck img{width:100%;height:100%;object-fit:contain;display:block;pointer-events:none}
.player{position:absolute;inset:0}
.vinyl{position:absolute;inset:0;will-change:transform}
.tonearm{position:absolute;inset:0;padding:0;border:0;background:transparent;cursor:grab;z-index:3;touch-action:none}
.tonearm:active,.tonearm.dragging{cursor:grabbing}
.tonearm.dragging{transition:none}
.tonearm:focus-visible{outline:0}

.move-prompt{position:absolute;right:0;bottom:8%;display:flex;align-items:center;gap:10px;background:var(--orange);color:var(--ink);border:0;border-radius:50%;width:145px;height:145px;justify-content:center;transform:rotate(-8deg);cursor:default;z-index:5;box-shadow:0 10px 45px #0008;transition:.25s;animation:nudge 1.8s ease-in-out infinite}
.move-prompt b{display:block;text-transform:uppercase;font-family:var(--display);font-size:20px;max-width:88px;text-align:center;line-height:1.05}
.playing+.move-prompt{opacity:0;visibility:hidden;transform:scale(.7)}

/* Final layer alignment used by the live component. */
.deck .player,.deck .tonearm{position:absolute;width:100%;height:100%;object-fit:contain}
.deck .player{z-index:1;inset:0}
.deck .vinyl-wrap{position:absolute;inset:0;z-index:2;transform:translate(-2.919%,-1.495%) scale(.632);transform-origin:50% 50%;pointer-events:none}
.deck .vinyl{position:absolute;inset:0;width:100%;height:100%;object-fit:contain;transform-origin:49.88% 49.12%}
.deck .tonearm{z-index:3;left:3.76%;top:7.42%;right:auto;bottom:auto;transform-origin:78.79% 27.15%;transform:rotate(-38deg) scale(.45);transition:transform .5s cubic-bezier(.22,.8,.2,1)}
.deck .start-stop-hit{position:absolute;z-index:5;left:8.1%;top:74.7%;width:10.3%;height:8.4%;border:0;background:transparent;cursor:pointer;border-radius:3px;box-shadow:0 0 0 0 rgba(255,113,53,0);transition:box-shadow .2s,transform .12s}
.deck .start-stop-hit:hover,.deck .start-stop-hit:focus-visible{outline:0;box-shadow:none}
.deck .start-stop-hit:active{transform:translateY(2px)}
.deck.playing .tonearm{transform:rotate(-15deg) scale(.45)}
.deck.playing .vinyl{animation:record-spin 3.8s linear infinite}

.home-page .deck-area{width:min(83.52vw,1094px)}
.home-page .hero{min-height:max(100vh,1220px)}

@keyframes record-spin{from{transform:rotate(0deg)}to{transform:rotate(360deg)}}
@keyframes nudge{50%{transform:rotate(-5deg) translateY(-5px)}}

@media(max-width:900px){
  .home-page .hero{grid-template-columns:minmax(0,1fr);align-content:start;min-height:980px}
  .home-page .deck-area{width:min(96vw,744px);justify-self:center;margin:8px auto 0}
}

@media(max-width:560px){
  .home-page .deck-area{width:78vw}
  .home-page .hero{padding-left:20px;padding-right:20px;min-height:920px}
  .move-prompt{display:flex;visibility:visible;opacity:1;width:78px;height:78px;left:45%;right:auto;bottom:4%}
  .move-prompt b{font-size:14px}
}

@media(min-width:901px){
  .home-page .hero{min-height:100vh;padding:105px 4vw 50px;grid-template-columns:minmax(270px,.78fr) minmax(520px,1.25fr)}
  .home-page .deck-area{width:min(47vw,640px,calc(100vh - 155px));margin-right:2vw}
  .home-page .move-prompt{width:112px;height:112px}
  .home-page .move-prompt b{font-size:17px}
}

@media(prefers-reduced-motion:reduce){
  .vinyl{animation:none!important}
}

CSS dependency note: --orange, --ink, and --display are project-wide CSS custom
properties/font settings. Copy their definitions from the destination design system,
or replace them with fixed colors and a font family.


5. JAVASCRIPT — VINYL PLAYER LOGIC (FROM script.js)
---------------------------------------------------

const deck = document.querySelector('#deck');
const tonearm = document.querySelector('#tonearm');
const audio = document.querySelector('#recordAudio');
const startStopButton = document.querySelector('#startStopButton');
const PARKED_ANGLE = -38;
const PLAYING_ANGLE = -15;
const STATE_THRESHOLD = -26;
const BASE_ARM_ANGLE = 136.4;
let currentAngle = PARKED_ANGLE;
let dragging = false;
let playing = false;
const PLAYBACK_KEY = 'naive-tattoo-playback';

if (audio) audio.volume = 0.4;

function savePlayback(active = playing && !audio?.paused) {
  if (!audio) return;
  sessionStorage.setItem(PLAYBACK_KEY, JSON.stringify({ active, time: audio.currentTime || 0 }));
}

function setArm(angle) {
  currentAngle = Math.max(PARKED_ANGLE, Math.min(PLAYING_ANGLE, angle));
  tonearm.style.transform = `rotate(${currentAngle}deg) scale(.45)`;
}

function angleFromPointer(event) {
  const rect = deck.getBoundingClientRect();
  const pivotX = rect.left + rect.width * .8255;
  const pivotY = rect.top + rect.height * .3457;
  return Math.atan2(event.clientY - pivotY, event.clientX - pivotX) * 180 / Math.PI - BASE_ARM_ANGLE;
}

async function playRecord() {
  playing = true;
  dragging = false;
  tonearm.classList.remove('dragging');
  tonearm.style.removeProperty('transform');
  deck.classList.add('playing');
  tonearm.setAttribute('aria-pressed', 'true');
  startStopButton?.setAttribute('aria-pressed', 'true');
  try { await audio.play(); savePlayback(true); }
  catch (error) { console.warn('Audio could not start:', error); }
}

function parkRecord() {
  playing = false;
  dragging = false;
  audio.pause();
  deck.classList.remove('playing');
  tonearm.classList.remove('dragging');
  tonearm.setAttribute('aria-pressed', 'false');
  startStopButton?.setAttribute('aria-pressed', 'false');
  tonearm.style.removeProperty('transform');
  currentAngle = PARKED_ANGLE;
  savePlayback(false);
}

if (tonearm) {
  tonearm.addEventListener('pointerdown', event => {
    dragging = true;
    tonearm.classList.add('dragging');
    tonearm.setPointerCapture(event.pointerId);
    setArm(angleFromPointer(event));
  });

  tonearm.addEventListener('pointermove', event => {
    if (dragging) setArm(angleFromPointer(event));
  });

  const releaseArm = () => {
    if (!dragging) return;
    dragging = false;
    tonearm.classList.remove('dragging');
    currentAngle >= STATE_THRESHOLD ? playRecord() : parkRecord();
  };

  tonearm.addEventListener('pointerup', releaseArm);
  tonearm.addEventListener('pointercancel', releaseArm);
  tonearm.addEventListener('keydown', event => {
    if (event.key === 'Enter' || event.key === ' ') {
      event.preventDefault();
      playing ? parkRecord() : playRecord();
    }
  });
}

if (startStopButton) {
  startStopButton.addEventListener('click', () => playing ? parkRecord() : playRecord());
}

if (audio) {
  let savedPlayback;
  try {
    savedPlayback = JSON.parse(sessionStorage.getItem(PLAYBACK_KEY) || 'null');
  } catch {
    savedPlayback = null;
  }

  if (savedPlayback?.active) {
    audio.currentTime = Number(savedPlayback.time) || 0;
    if (deck && tonearm) {
      playing = true;
      deck.classList.add('playing');
      tonearm.setAttribute('aria-pressed', 'true');
      startStopButton?.setAttribute('aria-pressed', 'true');
    }
    const resumeAudio = () => audio.play().then(() => savePlayback(true)).catch(() => {});
    resumeAudio();
    document.addEventListener('pointerdown', resumeAudio, { once: true });
  }

  audio.addEventListener('timeupdate', () => {
    if (!audio.paused) savePlayback(true);
  });
  window.addEventListener('pagehide', () => savePlayback(!audio.paused));
}


6. IMPORTANT POSITIONING VALUES
--------------------------------

- Vinyl wrapper: translate(-2.919%, -1.495%) scale(.632)
- Vinyl rotation center: 49.88% 49.12%
- Tonearm image placement: left 3.76%, top 7.42%
- Tonearm pivot: 78.79% 27.15%
- Tonearm artwork scale: .45
- Parked angle: -38 degrees
- Playing angle: -15 degrees
- Drag-release threshold: -26 degrees
- Pointer pivot used by JavaScript: 82.55% x, 34.57% y of the deck bounds
- Start/stop hit area: left 8.1%, top 74.7%, width 10.3%, height 8.4%
- Record rotation speed: 3.8 seconds per revolution
- Audio volume: 0.4 (40%)


7. FILES THAT MUST TRAVEL WITH THE COMPONENT
---------------------------------------------

Required for the live implementation:

- The HTML in section 3
- The CSS in section 4
- The JavaScript in section 5
- Assets/Vinyl player/player.png
- Assets/Vinyl player/vinyl.png
- Assets/Vinyl player/stick.png
- Assets/Music/Rock Wit U (Yoruba Soul Mix - Dub).mp3

Recommended reference/source files to keep with the handoff:

- Assets/Vinyl player/start stop button.png
- Assets/Vinyl player/Tattoo gun.png
- Assets/Vinyl player/Expected when off.png
- Assets/Vinyl player/Expected when on.png
- Assets/Vinyl player/assembly guide.png
- Assets/Vinyl player/Guide to assemble.png

